---
driver:
  name: docker

docker:
  containers:

    - name: ansible-zabbix-repo-ubuntu-xenial
      image: ubuntu
      image_version: xenial

    - name: ansible-zabbix-repo-ubuntu-trusty
      image: ubuntu
      image_version: trusty

    - name: ansible-zabbix-repo-centos-7
      image: centos
      image_version: 7

    - name: ansible-zabbix-repo-centos-6
      image: centos
      image_version: 6
